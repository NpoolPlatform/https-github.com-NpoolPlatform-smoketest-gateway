// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/smoketest-middleware/pkg/db/ent/plantestcase"
	"github.com/NpoolPlatform/smoketest-middleware/pkg/db/ent/predicate"
)

// PlanTestCaseQuery is the builder for querying PlanTestCase entities.
type PlanTestCaseQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.PlanTestCase
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlanTestCaseQuery builder.
func (ptcq *PlanTestCaseQuery) Where(ps ...predicate.PlanTestCase) *PlanTestCaseQuery {
	ptcq.predicates = append(ptcq.predicates, ps...)
	return ptcq
}

// Limit adds a limit step to the query.
func (ptcq *PlanTestCaseQuery) Limit(limit int) *PlanTestCaseQuery {
	ptcq.limit = &limit
	return ptcq
}

// Offset adds an offset step to the query.
func (ptcq *PlanTestCaseQuery) Offset(offset int) *PlanTestCaseQuery {
	ptcq.offset = &offset
	return ptcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ptcq *PlanTestCaseQuery) Unique(unique bool) *PlanTestCaseQuery {
	ptcq.unique = &unique
	return ptcq
}

// Order adds an order step to the query.
func (ptcq *PlanTestCaseQuery) Order(o ...OrderFunc) *PlanTestCaseQuery {
	ptcq.order = append(ptcq.order, o...)
	return ptcq
}

// First returns the first PlanTestCase entity from the query.
// Returns a *NotFoundError when no PlanTestCase was found.
func (ptcq *PlanTestCaseQuery) First(ctx context.Context) (*PlanTestCase, error) {
	nodes, err := ptcq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{plantestcase.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ptcq *PlanTestCaseQuery) FirstX(ctx context.Context) *PlanTestCase {
	node, err := ptcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PlanTestCase ID from the query.
// Returns a *NotFoundError when no PlanTestCase ID was found.
func (ptcq *PlanTestCaseQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ptcq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{plantestcase.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ptcq *PlanTestCaseQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := ptcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PlanTestCase entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PlanTestCase entity is found.
// Returns a *NotFoundError when no PlanTestCase entities are found.
func (ptcq *PlanTestCaseQuery) Only(ctx context.Context) (*PlanTestCase, error) {
	nodes, err := ptcq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{plantestcase.Label}
	default:
		return nil, &NotSingularError{plantestcase.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ptcq *PlanTestCaseQuery) OnlyX(ctx context.Context) *PlanTestCase {
	node, err := ptcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PlanTestCase ID in the query.
// Returns a *NotSingularError when more than one PlanTestCase ID is found.
// Returns a *NotFoundError when no entities are found.
func (ptcq *PlanTestCaseQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ptcq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{plantestcase.Label}
	default:
		err = &NotSingularError{plantestcase.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ptcq *PlanTestCaseQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := ptcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PlanTestCases.
func (ptcq *PlanTestCaseQuery) All(ctx context.Context) ([]*PlanTestCase, error) {
	if err := ptcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return ptcq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (ptcq *PlanTestCaseQuery) AllX(ctx context.Context) []*PlanTestCase {
	nodes, err := ptcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PlanTestCase IDs.
func (ptcq *PlanTestCaseQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := ptcq.Select(plantestcase.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ptcq *PlanTestCaseQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := ptcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ptcq *PlanTestCaseQuery) Count(ctx context.Context) (int, error) {
	if err := ptcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return ptcq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (ptcq *PlanTestCaseQuery) CountX(ctx context.Context) int {
	count, err := ptcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ptcq *PlanTestCaseQuery) Exist(ctx context.Context) (bool, error) {
	if err := ptcq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return ptcq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (ptcq *PlanTestCaseQuery) ExistX(ctx context.Context) bool {
	exist, err := ptcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlanTestCaseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ptcq *PlanTestCaseQuery) Clone() *PlanTestCaseQuery {
	if ptcq == nil {
		return nil
	}
	return &PlanTestCaseQuery{
		config:     ptcq.config,
		limit:      ptcq.limit,
		offset:     ptcq.offset,
		order:      append([]OrderFunc{}, ptcq.order...),
		predicates: append([]predicate.PlanTestCase{}, ptcq.predicates...),
		// clone intermediate query.
		sql:    ptcq.sql.Clone(),
		path:   ptcq.path,
		unique: ptcq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PlanTestCase.Query().
//		GroupBy(plantestcase.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (ptcq *PlanTestCaseQuery) GroupBy(field string, fields ...string) *PlanTestCaseGroupBy {
	grbuild := &PlanTestCaseGroupBy{config: ptcq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := ptcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return ptcq.sqlQuery(ctx), nil
	}
	grbuild.label = plantestcase.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.PlanTestCase.Query().
//		Select(plantestcase.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (ptcq *PlanTestCaseQuery) Select(fields ...string) *PlanTestCaseSelect {
	ptcq.fields = append(ptcq.fields, fields...)
	selbuild := &PlanTestCaseSelect{PlanTestCaseQuery: ptcq}
	selbuild.label = plantestcase.Label
	selbuild.flds, selbuild.scan = &ptcq.fields, selbuild.Scan
	return selbuild
}

func (ptcq *PlanTestCaseQuery) prepareQuery(ctx context.Context) error {
	for _, f := range ptcq.fields {
		if !plantestcase.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ptcq.path != nil {
		prev, err := ptcq.path(ctx)
		if err != nil {
			return err
		}
		ptcq.sql = prev
	}
	if plantestcase.Policy == nil {
		return errors.New("ent: uninitialized plantestcase.Policy (forgotten import ent/runtime?)")
	}
	if err := plantestcase.Policy.EvalQuery(ctx, ptcq); err != nil {
		return err
	}
	return nil
}

func (ptcq *PlanTestCaseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PlanTestCase, error) {
	var (
		nodes = []*PlanTestCase{}
		_spec = ptcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*PlanTestCase).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &PlanTestCase{config: ptcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(ptcq.modifiers) > 0 {
		_spec.Modifiers = ptcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ptcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ptcq *PlanTestCaseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ptcq.querySpec()
	if len(ptcq.modifiers) > 0 {
		_spec.Modifiers = ptcq.modifiers
	}
	_spec.Node.Columns = ptcq.fields
	if len(ptcq.fields) > 0 {
		_spec.Unique = ptcq.unique != nil && *ptcq.unique
	}
	return sqlgraph.CountNodes(ctx, ptcq.driver, _spec)
}

func (ptcq *PlanTestCaseQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := ptcq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (ptcq *PlanTestCaseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   plantestcase.Table,
			Columns: plantestcase.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: plantestcase.FieldID,
			},
		},
		From:   ptcq.sql,
		Unique: true,
	}
	if unique := ptcq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := ptcq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, plantestcase.FieldID)
		for i := range fields {
			if fields[i] != plantestcase.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ptcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ptcq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ptcq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ptcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ptcq *PlanTestCaseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ptcq.driver.Dialect())
	t1 := builder.Table(plantestcase.Table)
	columns := ptcq.fields
	if len(columns) == 0 {
		columns = plantestcase.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ptcq.sql != nil {
		selector = ptcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ptcq.unique != nil && *ptcq.unique {
		selector.Distinct()
	}
	for _, m := range ptcq.modifiers {
		m(selector)
	}
	for _, p := range ptcq.predicates {
		p(selector)
	}
	for _, p := range ptcq.order {
		p(selector)
	}
	if offset := ptcq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ptcq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ptcq *PlanTestCaseQuery) ForUpdate(opts ...sql.LockOption) *PlanTestCaseQuery {
	if ptcq.driver.Dialect() == dialect.Postgres {
		ptcq.Unique(false)
	}
	ptcq.modifiers = append(ptcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ptcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ptcq *PlanTestCaseQuery) ForShare(opts ...sql.LockOption) *PlanTestCaseQuery {
	if ptcq.driver.Dialect() == dialect.Postgres {
		ptcq.Unique(false)
	}
	ptcq.modifiers = append(ptcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ptcq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ptcq *PlanTestCaseQuery) Modify(modifiers ...func(s *sql.Selector)) *PlanTestCaseSelect {
	ptcq.modifiers = append(ptcq.modifiers, modifiers...)
	return ptcq.Select()
}

// PlanTestCaseGroupBy is the group-by builder for PlanTestCase entities.
type PlanTestCaseGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ptcgb *PlanTestCaseGroupBy) Aggregate(fns ...AggregateFunc) *PlanTestCaseGroupBy {
	ptcgb.fns = append(ptcgb.fns, fns...)
	return ptcgb
}

// Scan applies the group-by query and scans the result into the given value.
func (ptcgb *PlanTestCaseGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := ptcgb.path(ctx)
	if err != nil {
		return err
	}
	ptcgb.sql = query
	return ptcgb.sqlScan(ctx, v)
}

func (ptcgb *PlanTestCaseGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range ptcgb.fields {
		if !plantestcase.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := ptcgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ptcgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (ptcgb *PlanTestCaseGroupBy) sqlQuery() *sql.Selector {
	selector := ptcgb.sql.Select()
	aggregation := make([]string, 0, len(ptcgb.fns))
	for _, fn := range ptcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(ptcgb.fields)+len(ptcgb.fns))
		for _, f := range ptcgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(ptcgb.fields...)...)
}

// PlanTestCaseSelect is the builder for selecting fields of PlanTestCase entities.
type PlanTestCaseSelect struct {
	*PlanTestCaseQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (ptcs *PlanTestCaseSelect) Scan(ctx context.Context, v interface{}) error {
	if err := ptcs.prepareQuery(ctx); err != nil {
		return err
	}
	ptcs.sql = ptcs.PlanTestCaseQuery.sqlQuery(ctx)
	return ptcs.sqlScan(ctx, v)
}

func (ptcs *PlanTestCaseSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := ptcs.sql.Query()
	if err := ptcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ptcs *PlanTestCaseSelect) Modify(modifiers ...func(s *sql.Selector)) *PlanTestCaseSelect {
	ptcs.modifiers = append(ptcs.modifiers, modifiers...)
	return ptcs
}
