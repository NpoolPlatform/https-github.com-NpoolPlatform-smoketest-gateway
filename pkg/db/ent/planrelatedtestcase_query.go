// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/smoketest-middleware/pkg/db/ent/planrelatedtestcase"
	"github.com/NpoolPlatform/smoketest-middleware/pkg/db/ent/predicate"
	"github.com/google/uuid"
)

// PlanRelatedTestCaseQuery is the builder for querying PlanRelatedTestCase entities.
type PlanRelatedTestCaseQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.PlanRelatedTestCase
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlanRelatedTestCaseQuery builder.
func (prtcq *PlanRelatedTestCaseQuery) Where(ps ...predicate.PlanRelatedTestCase) *PlanRelatedTestCaseQuery {
	prtcq.predicates = append(prtcq.predicates, ps...)
	return prtcq
}

// Limit adds a limit step to the query.
func (prtcq *PlanRelatedTestCaseQuery) Limit(limit int) *PlanRelatedTestCaseQuery {
	prtcq.limit = &limit
	return prtcq
}

// Offset adds an offset step to the query.
func (prtcq *PlanRelatedTestCaseQuery) Offset(offset int) *PlanRelatedTestCaseQuery {
	prtcq.offset = &offset
	return prtcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (prtcq *PlanRelatedTestCaseQuery) Unique(unique bool) *PlanRelatedTestCaseQuery {
	prtcq.unique = &unique
	return prtcq
}

// Order adds an order step to the query.
func (prtcq *PlanRelatedTestCaseQuery) Order(o ...OrderFunc) *PlanRelatedTestCaseQuery {
	prtcq.order = append(prtcq.order, o...)
	return prtcq
}

// First returns the first PlanRelatedTestCase entity from the query.
// Returns a *NotFoundError when no PlanRelatedTestCase was found.
func (prtcq *PlanRelatedTestCaseQuery) First(ctx context.Context) (*PlanRelatedTestCase, error) {
	nodes, err := prtcq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{planrelatedtestcase.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (prtcq *PlanRelatedTestCaseQuery) FirstX(ctx context.Context) *PlanRelatedTestCase {
	node, err := prtcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PlanRelatedTestCase ID from the query.
// Returns a *NotFoundError when no PlanRelatedTestCase ID was found.
func (prtcq *PlanRelatedTestCaseQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = prtcq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{planrelatedtestcase.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (prtcq *PlanRelatedTestCaseQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := prtcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PlanRelatedTestCase entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PlanRelatedTestCase entity is found.
// Returns a *NotFoundError when no PlanRelatedTestCase entities are found.
func (prtcq *PlanRelatedTestCaseQuery) Only(ctx context.Context) (*PlanRelatedTestCase, error) {
	nodes, err := prtcq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{planrelatedtestcase.Label}
	default:
		return nil, &NotSingularError{planrelatedtestcase.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (prtcq *PlanRelatedTestCaseQuery) OnlyX(ctx context.Context) *PlanRelatedTestCase {
	node, err := prtcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PlanRelatedTestCase ID in the query.
// Returns a *NotSingularError when more than one PlanRelatedTestCase ID is found.
// Returns a *NotFoundError when no entities are found.
func (prtcq *PlanRelatedTestCaseQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = prtcq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{planrelatedtestcase.Label}
	default:
		err = &NotSingularError{planrelatedtestcase.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (prtcq *PlanRelatedTestCaseQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := prtcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PlanRelatedTestCases.
func (prtcq *PlanRelatedTestCaseQuery) All(ctx context.Context) ([]*PlanRelatedTestCase, error) {
	if err := prtcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return prtcq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (prtcq *PlanRelatedTestCaseQuery) AllX(ctx context.Context) []*PlanRelatedTestCase {
	nodes, err := prtcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PlanRelatedTestCase IDs.
func (prtcq *PlanRelatedTestCaseQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := prtcq.Select(planrelatedtestcase.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (prtcq *PlanRelatedTestCaseQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := prtcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (prtcq *PlanRelatedTestCaseQuery) Count(ctx context.Context) (int, error) {
	if err := prtcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return prtcq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (prtcq *PlanRelatedTestCaseQuery) CountX(ctx context.Context) int {
	count, err := prtcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (prtcq *PlanRelatedTestCaseQuery) Exist(ctx context.Context) (bool, error) {
	if err := prtcq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return prtcq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (prtcq *PlanRelatedTestCaseQuery) ExistX(ctx context.Context) bool {
	exist, err := prtcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlanRelatedTestCaseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (prtcq *PlanRelatedTestCaseQuery) Clone() *PlanRelatedTestCaseQuery {
	if prtcq == nil {
		return nil
	}
	return &PlanRelatedTestCaseQuery{
		config:     prtcq.config,
		limit:      prtcq.limit,
		offset:     prtcq.offset,
		order:      append([]OrderFunc{}, prtcq.order...),
		predicates: append([]predicate.PlanRelatedTestCase{}, prtcq.predicates...),
		// clone intermediate query.
		sql:    prtcq.sql.Clone(),
		path:   prtcq.path,
		unique: prtcq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PlanRelatedTestCase.Query().
//		GroupBy(planrelatedtestcase.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (prtcq *PlanRelatedTestCaseQuery) GroupBy(field string, fields ...string) *PlanRelatedTestCaseGroupBy {
	grbuild := &PlanRelatedTestCaseGroupBy{config: prtcq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := prtcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return prtcq.sqlQuery(ctx), nil
	}
	grbuild.label = planrelatedtestcase.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.PlanRelatedTestCase.Query().
//		Select(planrelatedtestcase.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (prtcq *PlanRelatedTestCaseQuery) Select(fields ...string) *PlanRelatedTestCaseSelect {
	prtcq.fields = append(prtcq.fields, fields...)
	selbuild := &PlanRelatedTestCaseSelect{PlanRelatedTestCaseQuery: prtcq}
	selbuild.label = planrelatedtestcase.Label
	selbuild.flds, selbuild.scan = &prtcq.fields, selbuild.Scan
	return selbuild
}

func (prtcq *PlanRelatedTestCaseQuery) prepareQuery(ctx context.Context) error {
	for _, f := range prtcq.fields {
		if !planrelatedtestcase.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if prtcq.path != nil {
		prev, err := prtcq.path(ctx)
		if err != nil {
			return err
		}
		prtcq.sql = prev
	}
	if planrelatedtestcase.Policy == nil {
		return errors.New("ent: uninitialized planrelatedtestcase.Policy (forgotten import ent/runtime?)")
	}
	if err := planrelatedtestcase.Policy.EvalQuery(ctx, prtcq); err != nil {
		return err
	}
	return nil
}

func (prtcq *PlanRelatedTestCaseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PlanRelatedTestCase, error) {
	var (
		nodes = []*PlanRelatedTestCase{}
		_spec = prtcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*PlanRelatedTestCase).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &PlanRelatedTestCase{config: prtcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(prtcq.modifiers) > 0 {
		_spec.Modifiers = prtcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, prtcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (prtcq *PlanRelatedTestCaseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := prtcq.querySpec()
	if len(prtcq.modifiers) > 0 {
		_spec.Modifiers = prtcq.modifiers
	}
	_spec.Node.Columns = prtcq.fields
	if len(prtcq.fields) > 0 {
		_spec.Unique = prtcq.unique != nil && *prtcq.unique
	}
	return sqlgraph.CountNodes(ctx, prtcq.driver, _spec)
}

func (prtcq *PlanRelatedTestCaseQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := prtcq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (prtcq *PlanRelatedTestCaseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   planrelatedtestcase.Table,
			Columns: planrelatedtestcase.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: planrelatedtestcase.FieldID,
			},
		},
		From:   prtcq.sql,
		Unique: true,
	}
	if unique := prtcq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := prtcq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, planrelatedtestcase.FieldID)
		for i := range fields {
			if fields[i] != planrelatedtestcase.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := prtcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := prtcq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := prtcq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := prtcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (prtcq *PlanRelatedTestCaseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(prtcq.driver.Dialect())
	t1 := builder.Table(planrelatedtestcase.Table)
	columns := prtcq.fields
	if len(columns) == 0 {
		columns = planrelatedtestcase.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if prtcq.sql != nil {
		selector = prtcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if prtcq.unique != nil && *prtcq.unique {
		selector.Distinct()
	}
	for _, m := range prtcq.modifiers {
		m(selector)
	}
	for _, p := range prtcq.predicates {
		p(selector)
	}
	for _, p := range prtcq.order {
		p(selector)
	}
	if offset := prtcq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := prtcq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (prtcq *PlanRelatedTestCaseQuery) ForUpdate(opts ...sql.LockOption) *PlanRelatedTestCaseQuery {
	if prtcq.driver.Dialect() == dialect.Postgres {
		prtcq.Unique(false)
	}
	prtcq.modifiers = append(prtcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return prtcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (prtcq *PlanRelatedTestCaseQuery) ForShare(opts ...sql.LockOption) *PlanRelatedTestCaseQuery {
	if prtcq.driver.Dialect() == dialect.Postgres {
		prtcq.Unique(false)
	}
	prtcq.modifiers = append(prtcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return prtcq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (prtcq *PlanRelatedTestCaseQuery) Modify(modifiers ...func(s *sql.Selector)) *PlanRelatedTestCaseSelect {
	prtcq.modifiers = append(prtcq.modifiers, modifiers...)
	return prtcq.Select()
}

// PlanRelatedTestCaseGroupBy is the group-by builder for PlanRelatedTestCase entities.
type PlanRelatedTestCaseGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (prtcgb *PlanRelatedTestCaseGroupBy) Aggregate(fns ...AggregateFunc) *PlanRelatedTestCaseGroupBy {
	prtcgb.fns = append(prtcgb.fns, fns...)
	return prtcgb
}

// Scan applies the group-by query and scans the result into the given value.
func (prtcgb *PlanRelatedTestCaseGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := prtcgb.path(ctx)
	if err != nil {
		return err
	}
	prtcgb.sql = query
	return prtcgb.sqlScan(ctx, v)
}

func (prtcgb *PlanRelatedTestCaseGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range prtcgb.fields {
		if !planrelatedtestcase.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := prtcgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prtcgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (prtcgb *PlanRelatedTestCaseGroupBy) sqlQuery() *sql.Selector {
	selector := prtcgb.sql.Select()
	aggregation := make([]string, 0, len(prtcgb.fns))
	for _, fn := range prtcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(prtcgb.fields)+len(prtcgb.fns))
		for _, f := range prtcgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(prtcgb.fields...)...)
}

// PlanRelatedTestCaseSelect is the builder for selecting fields of PlanRelatedTestCase entities.
type PlanRelatedTestCaseSelect struct {
	*PlanRelatedTestCaseQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (prtcs *PlanRelatedTestCaseSelect) Scan(ctx context.Context, v interface{}) error {
	if err := prtcs.prepareQuery(ctx); err != nil {
		return err
	}
	prtcs.sql = prtcs.PlanRelatedTestCaseQuery.sqlQuery(ctx)
	return prtcs.sqlScan(ctx, v)
}

func (prtcs *PlanRelatedTestCaseSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := prtcs.sql.Query()
	if err := prtcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (prtcs *PlanRelatedTestCaseSelect) Modify(modifiers ...func(s *sql.Selector)) *PlanRelatedTestCaseSelect {
	prtcs.modifiers = append(prtcs.modifiers, modifiers...)
	return prtcs
}
